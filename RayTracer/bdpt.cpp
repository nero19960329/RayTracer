#include "bdpt.h"

glm::dvec3 BidirectionalPathTracing::color(const Ray & ray, RNG * rng) const {
	return glm::dvec3();
}

glm::dvec3 BidirectionalPathTracing::getColor(DistRay & ray, RNG * rng, int depth) const {
	return glm::dvec3();
}
